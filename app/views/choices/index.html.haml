= render 'layouts/header', title: 'Choices', small_title: @decision.question

- if @choices.any?
  %ul.nav.nav-tabs.nav-stacked
    = render @choices
  .center= will_paginate

- else
  %h4.center You'll need to have some choices in order to make a decision. 
  %p.center Click the button below to create a new one.

= render layout: 'layouts/action_bar', locals: { back_path: @decision, action_path: new_decision_choice_path(@decision) } do
  New choice
  %i.icon-plus.icon-white