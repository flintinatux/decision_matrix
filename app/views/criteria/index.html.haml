= render 'layouts/header', title: 'Criteria', small_title: @decision.question

- if @criteria.any?
  %ul.nav.nav-tabs.nav-stacked
    = render @criteria

- else
  %h4.center You'll need to have at least one criterion in order to make a decision.
  %p.center (Although more would be better!)
  %p.center Click the button below to create a new one.

= render layout: 'layouts/action_bar', locals: { back_path: @decision, action_path: new_decision_criterion_path(@decision) } do
  New criterion
  %i.icon-plus.icon-white